import copy
copy.deepcopy("lil")

#copy just copies with references but deep copies copy the code within an object. If you want to
#change something without changing it you can make a copy and change the copy, this can be useful.